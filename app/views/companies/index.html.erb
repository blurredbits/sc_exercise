<h1>Companies</h1>
<ul class="actions">
  <li>
    <%= link_to "New Company", new_company_path, class: "new" %>
  </li>
</ul>

<div id="companies">
  <% @companies.each do |company| %>
    <h2><%= link_to company.name, company %></h2>
  <% end %>
</div>